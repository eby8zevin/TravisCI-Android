language: android
sudo: required
jdk: oraclejdk8
#dist: trusty
#os: linux

android:
  components:
    - tools
    - platform-tools
    - tools
    - build-tools-30.0.3
    - android-30
    - extra-android-m2repository

#optional, use it if only you haven't give permission yet.
#before_install:
#   - chmod +x gradlew
  
script:
   - ./gradlew build
   - ./gradlew clean
